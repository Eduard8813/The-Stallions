spring.application.name=wani connect

spring.datasource.url=jdbc:sqlserver://The_stallions.mssql.somee.com:1433;databaseName=The_stallions;encrypt=true;trustServerCertificate=true
spring.datasource.username=Eduard_SQLLogin_1
spring.datasource.password=Eduard10@$
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

jwt.secret=my-super-secret-key-change-this-in-production-min-32-chars
jwt.expiration-ms=3600000

firebase.credentials-path=firebase-credentials.json

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Base URL pública del backend, usada para construir la URL de la foto de perfil.
# Si no se define, se toma del host de la petición.
app.base-url=

# Credenciales del panel de administración (/admin/**).
# Cambiar en producción (o usar variables de entorno APP_ADMIN_USERNAME / APP_ADMIN_PASSWORD).
app.admin.username=admin
app.admin.password=admin123
